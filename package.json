{
  "name": "ejalas_testing",
  "version": "1.0.0",
  "description": "",
  "main": "loginTesting.js",
  "scripts": {
    "test": "npx mocha --no-timeout --parallel --reporter mochawesome --require mochawesome/register ",
    "recursive": "npx mocha --no-timeout --parallel --reporter mochawesome --require mochawesome/register -recursive ./tests"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^5.0.0",
    "cherio": "^1.0.0-rc.2",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.16.0"
  }
}